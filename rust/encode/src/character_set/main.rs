#![allow(dead_code)]

mod hexupper;
mod url_encode;
mod x_www_form_urlencode;
mod base64;
fn main() {
    // url_encode::new();
    // x_www_form_urlencode::new();
    // hexupper::new();
    base64::new();
}
