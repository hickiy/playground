#![allow(dead_code)]
#![allow(unexpected_cfgs)]
mod assert;
mod compute;
mod search;
mod vec_sort;
mod map_reduce;
mod thumbnial_jpg;

fn main() {
    // compute::new();
    // assert::new();
    // search::new();
    // vec_sort::new();
    // map_reduce::new();
    thumbnial_jpg::new();
}
