#![allow(dead_code)]
mod date_check;
mod date_interconversion_timestamp;
mod format;
mod measuring_time;
mod time_check;
mod timezone_conversion;
mod parse_to_struct_form_str;
fn main() {
    // measuring_time::new();
    // date_check::new();
    // timezone_conversion::new();
    // time_check::new();
    // date_interconversion_timestamp::new();
    // format::new();
    parse_to_struct_form_str::new();
}
